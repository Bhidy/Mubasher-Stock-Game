# Cloudflare Pages Redirects Configuration
# SPA routing and optimizations

# SPA fallback - all routes serve index.html
/*  /index.html  200

# API routes pass through (handled by functions)
/api/*  /api/:splat  200
